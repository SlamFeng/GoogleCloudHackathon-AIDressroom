**Hairline** — a crisp 1px divider on the dark glass surface, used instead of shadows or heavy borders.

```jsx
<Hairline />
<Hairline vertical />
```

Props: `vertical` (stretches to parent height), `inset` (px margin along its length). Uses the low-alpha light hairline token so it reads on ink.
