**LanguageSwitch** — trilingual EN / 中文 / 日本語 glass pill. Active fills cobalt. The demo runs in Chinese (`zh` default).

```jsx
<LanguageSwitch value={lang} onChange={setLang} />
```

Small enough to live as a top control. Pass custom `langs` to reorder or relabel.
