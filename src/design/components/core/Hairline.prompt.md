**Hairline** — the 1px `#E6E6E6` divider that does Fashini's structural work in place of shadows.

```jsx
<Hairline />
<Hairline vertical />
```

`vertical` for column dividers; `inset` trims the ends.
