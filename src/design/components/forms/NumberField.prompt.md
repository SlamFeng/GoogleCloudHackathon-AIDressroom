**NumberField** — a big editorial numeric input with a trailing unit; for height / weight capture.

```jsx
<NumberField value={height} onChange={setHeight} unit="cm" min={100} max={230} />
```

The value renders large in the display grotesk over an ink baseline rule.
