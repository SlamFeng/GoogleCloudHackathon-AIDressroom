**PrivacyChip** — quiet dot-and-line privacy reassurance in the top bar / panels.

```jsx
<PrivacyChip>Photos deleted after this session</PrivacyChip>
<PrivacyChip>写真はこのセッション後に削除</PrivacyChip>
```

The dot uses neutral grey (`--ok`), never green.
