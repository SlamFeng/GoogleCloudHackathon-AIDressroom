**Checkbox** — square consent checkbox with the brand ✓; checked fills ink, not cobalt.

```jsx
<Checkbox checked={cameraConsent} onChange={setCameraConsent}>
  I agree to enable the camera and capture one front-facing full-body photo.
</Checkbox>
```

Used for the consent-gate rows before the camera opens.
