**LanguageSwitch** — the EN / 中文 / 日本語 toggle in the top bar; active fills ink.

```jsx
<LanguageSwitch value={lang} onChange={setLang} />
```

Fashini is trilingual-native; every screen ships all three languages.
