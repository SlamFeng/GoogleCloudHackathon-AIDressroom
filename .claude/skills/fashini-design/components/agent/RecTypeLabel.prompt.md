**RecTypeLabel** — the cobalt rec-type eyebrow on a recommendation set (one of the few accent uses).

```jsx
<RecTypeLabel recType={set.rec_type} />   {/* explicit_need → "Explicit need" */}
```

Binds to `RecommendationSet.rec_type`: explicit_need · similar · style · seasonal.
